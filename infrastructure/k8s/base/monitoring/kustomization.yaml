# Monitoring Stack Kustomization
# Note: Components are installed via Helm. This kustomization manages additional resources.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml

commonLabels:
  app.kubernetes.io/part-of: flexobo-monitoring
  app.kubernetes.io/managed-by: kustomize
