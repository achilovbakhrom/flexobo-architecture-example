apiVersion: v1
kind: Service
metadata:
  name: file-service
  namespace: flexobo
  labels:
    app: file-service
spec:
  type: ClusterIP
  ports:
    - port: 3007
      targetPort: 3007
      name: http
  selector:
    app: file-service
