# Rancher Kustomization
# Note: Rancher is installed via Helm. This manages namespace only.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml

commonLabels:
  app.kubernetes.io/part-of: flexobo-management
  app.kubernetes.io/managed-by: kustomize
