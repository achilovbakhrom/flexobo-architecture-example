apiVersion: v1
kind: Service
metadata:
  name: chat-service
  namespace: flexobo
  labels:
    app: chat-service
spec:
  type: ClusterIP
  ports:
    - port: 3006
      targetPort: 3006
      name: http
  selector:
    app: chat-service
