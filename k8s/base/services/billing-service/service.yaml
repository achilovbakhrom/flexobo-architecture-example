apiVersion: v1
kind: Service
metadata:
  name: billing-service
  namespace: flexobo
  labels:
    app: billing-service
spec:
  type: ClusterIP
  ports:
    - port: 3009
      targetPort: 3009
      name: http
  selector:
    app: billing-service
