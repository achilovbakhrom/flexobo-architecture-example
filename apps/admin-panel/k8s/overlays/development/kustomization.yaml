apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: admin-panel-dev

resources:
  - ../../base

namePrefix: dev-

patches:
  - patch: |-
      - op: replace
        path: /spec/replicas
        value: 1
    target:
      kind: Deployment
      name: admin-panel

configMapGenerator:
  - name: admin-panel-config
    behavior: merge
    literals:
      - NODE_ENV=development
      - LOG_LEVEL=debug
